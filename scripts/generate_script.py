import random\n\nclass PodcastScriptGenerator:\n    def __init__(self, topics):\n        self.topics = topics\n\n    def generate_episode_title(self):\n        # Generate a random episode title from a list of operations topics\n        topic = random.choice(self.topics)\n        return f"Daily Episode: Exploring {topic}"\n\n    def generate_script(self):\n        # Generate a simple script for the episode\n        title = self.generate_episode_title()\n        intro = f"Welcome to the Streamliners Podcast! Today's episode is titled '{title}'."\n        body = f"In this episode, we will delve into {topic} and explore its importance to operations in today's world."\n        outro = "Thank you for listening! Stay tuned for our next episode."\n\n        return f"{intro}\n\n{body}\n\n{outro}"\n\nif __name__ == '__main__':\n    topics = ["Lean Management", "Six Sigma", "Just-in-Time Production", "Total Quality Management"]\n    generator = PodcastScriptGenerator(topics)\n    script = generator.generate_script()\n    print(script)\n